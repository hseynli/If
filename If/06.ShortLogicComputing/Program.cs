int MIN_VALUE = 1;
int denominator = 0;
int item = 2;

if ((denominator != 0) && (item / denominator) > MIN_VALUE) // & operatorunu əvəz etsək, onda proqramda xəta baş verəcək
{
    Console.WriteLine("IF BLOCK");
}
else
{
    Console.WriteLine("ELSE BLOCK");
}